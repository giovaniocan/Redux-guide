import { CartActionTypes } from "./action-types"

const initialState = {
    products: [],
    productsTotalPric: 0

}


export const cartReducer = (state= initialState, action) => {
    switch(action.type) {

           

  

           

           
        default:
            return state
    }
}